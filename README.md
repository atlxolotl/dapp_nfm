# Non Fungible Messages DAPP

This is the frontend static site which works with lit and ipfs (decentralizable)

# Requirements

## Run with deno

```sh
deno run --allow-read=. --allow-net server.ts
```





